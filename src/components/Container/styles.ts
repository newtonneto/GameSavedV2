import { StyleSheet } from 'react-native';

import { JET } from '../../utils/theme';

const styles = StyleSheet.create({
  container: { backgroundColor: JET, width: '100%', height: '100%' },
});

export default styles;
