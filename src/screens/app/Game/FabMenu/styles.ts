import { StyleSheet } from 'react-native';

import { DIM_GRAY, COSMIT_LATTE, CORAL } from '../../../../utils/theme';

const styles = StyleSheet.create({
  menu: {
    backgroundColor: DIM_GRAY,
  },
  menuLabel: {
    color: COSMIT_LATTE,
  },
  fab: {
    backgroundColor: CORAL,
  },
});

export default styles;
